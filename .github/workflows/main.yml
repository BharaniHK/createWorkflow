name: Check For Changes to Monitored Files

on:
  push:
    branches: 
       - main
       
jobs:
  check_changes_and_alert:
    runs-on: ubuntu-latest

    steps: 
    - name: Checkout code
      uses: actions/checkout@v3 
      with:
       fetch-depth: ${{ github.event_name == 'pull_request' && 2 || 0 }}

    - name: Check and Alert if watched files changed
      id: check-and-alert
      run: |
         changed_files=$(git diff --name-only ${{ github.event.before }} ${{ github.event.after }})

         for file in $changed_files; do
            if [[ $file == *.html || $file == *.jsx ]]; then
              if git diff ${{ github.event.before }} ${{ github.sha }} -- "$file" | grep -q 'name='; then
                echo "Class attribute changed in $file"
                exit 1 # Exit with an error code if changes are found
              fi
            fi
          done
